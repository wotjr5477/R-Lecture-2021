---
title: "chapter 3연습문제 p.90"
author: "Junseok Ban"
date: '2021 4 21 '
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## 1. x=c(1:5)인 벡터가 있다. 이 벡터에 c(6:10)인 벡터를 추가하라.

```{r}
x = c(1:5);x
append(x,c(6:10))
```

## 1. x=c(1:10)인 벡터에서 짝수 번째 요소만 남긴 x를 만들어라.

```{r}
x = c(1:10);x
x[seq(2,10,2)]
```
